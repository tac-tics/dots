if [[ -x "$(which doctl 2> /dev/null)" ]]
then
    source <(doctl completion bash)
fi


if [[ -x "$(which kubectl 2> /dev/null)" ]]
then
    source <(kubectl completion bash)
fi

if [[ -x "$(which helm)" ]]
then
    source <(helm completion bash)
fi
