[user]
    name = Michael Maloney
    # email = tac@tac-tics.net

[core]
    editor = vim

[alias]
    graph = log --graph --decorate
    undo = reset --soft HEAD~
    commitsof = !git log "${1}...${1}^1"

[apply]
    whitespace = nowarn

[push]
    default = simple

[pull]
    rebase = true
